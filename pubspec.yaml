name: signal_scope
description: "RTL-SDR Radio Signal Analyzer with AI Detection"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  web_socket_channel: ^3.0.1
  provider: ^6.1.2
  fl_chart: ^0.69.0
  http: ^1.2.2
  shared_preferences: ^2.3.3
  flutter_tts: ^4.2.0
  msgpack_dart: ^1.0.0
  flutter_local_notifications: ^18.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
